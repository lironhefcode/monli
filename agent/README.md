# Agent (planned)

Rust (async, via `tokio`), distributed as a static binary. Not yet
implemented — see [PROJECT_PLAN.md](../PROJECT_PLAN.md) for the intended
design (collectors, scheduler, API client).
